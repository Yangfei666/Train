<?php

namespace App;

use App\Model;

class School extends Model
{
    //protected $table = 'schools';//对应表名
    //protected $primaryKey = 'id';//指定主键,默认id
    //public $incrementing = false;//非递增或者非数字的主键做测设置
    //protected $keyType = string;//如果主键不是一个整数，则设置
    //public $timestamps = false;//数据表中不存在 created_at 和 updated_at 这两个字段
    //const CREATED_AT = 'creation_date';//自定义创建时间字段
    //const UPDATED_AT = 'last_update';//自定义修改时间字段
}
